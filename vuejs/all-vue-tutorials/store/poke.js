/*
 포켓몬 관련 APP 의 Store

*/

// State
export const state = () => ({
})

// Mutations
export const mutations = {

}

// Getters
export const getters = {

}

// Actions

export const actions = {
    /* // actions Sample
    increment: context => {
        context.commit('increment');
    },
    // 아래의 버전은 위의 action 의 간략화된 버전이다. 
    // {} 문법은 객체를 분해하고 원하는 이름의 데이터를 가져온다.
    increment: ({ commit }, payload) => {
        commit('increment', payload);
    },
    decrement: ({ commit }) => {
        commit('decrement');
    }
    // 비동기 기능 추가
    asyncIncrement: ({commit}) => {
        setTimeout(() => {
            commit('increment');
        }, 1000 // 1초 후에 commit 하라.
        );
    },
    asyncDecrement: ({commit}) => {
        setTimeout(() => {
            commit('decrement');
        }, 1000 // 1초 후에 commit 하라.
        );
    }
    */
}